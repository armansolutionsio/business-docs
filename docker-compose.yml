# Monorepo root docker-compose
# Runs the full stack by including service-level compose files.
# Usage: docker compose up  (from repo root)
# Or run a specific app:   docker compose -f infra/docker-compose.yml up

include:
  - infra/docker-compose.yml
